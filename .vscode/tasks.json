{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Flash-EFG",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "windows": {
                "command": "${cwd}\\flash.bat"
            }
        }
    ]
}