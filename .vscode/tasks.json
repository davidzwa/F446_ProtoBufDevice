{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Flash-EFG",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "windows": {
                "command": "${cwd}\\tools\\flash.bat"
            }
        },
        {
            "label": "Protoc",
            "type": "shell",
            "group": "build",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "windows": {
                "command": "${cwd}\\generate_source_files.bat"
            }
        }
    ]
}
